
&НаСервере
Процедура ТСД_ПриЧтенииНаСервереПосле(ТекущийОбъект)
	ЭтаФорма.ТолькоПросмотр = ЗначениеЗаполнено(УправлениеСвойствами.ЗначениеСвойства(ТекущийОбъект.Ссылка, "ИД_ТСД_0d1b649d93a64607aa8ecccbfcfcdf43"));
	Если ЭтаФорма.ТолькоПросмотр Тогда
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = "Документ обрабатывается на ТСД";
		Сообщение.Поле = "Статус";
		Сообщение.УстановитьДанные(ТекущийОбъект);
		Сообщение.Сообщить();
	КонецЕсли;
КонецПроцедуры
